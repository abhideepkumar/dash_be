# PostgreSQL (user-provided at runtime via API)
# These are NOT stored here, only in session memory

# Pinecone
PINECONE_API_KEY=your_pinecone_api_key
PINECONE_INDEX_NAME=dashboard-ai-schema

# Google Gemini (for metadata generation AND embeddings)
GEMINI_API_KEY=AIzaSyCpMEWkz7K7gC-NS7HshjoE-Pb0UUcn6T8
# Server
PORT=8000
